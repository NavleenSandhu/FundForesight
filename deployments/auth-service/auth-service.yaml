apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
  namespace: fund-foresight
spec:
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP